#include<stdio.h>
int main()
{
	char a=getchar();
	printf("%c",a);
	return 0;

}


#include <stdio.h>
int main() {
    char ch;
    printf("Enter a single character: ");
    ch = getchar();  // 读取一个字符
    printf("You entered: %c\n", ch);
    return 0;
}
